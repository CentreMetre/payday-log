import type {Challenge} from "../models/challenge";
import type {ChallengeInstanceSubmitShape} from "../models/challenge-instance";


export function generateChallengeInstancesSqlInsertStatement(challengeInstances: ChallengeInstanceSubmitShape[]): string {
    const tableName = "challenge_instances";

    return "";
}